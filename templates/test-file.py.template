"""
Auto-generated by agent under LLM_OPERATING_GUIDE v2.0.
Phase: {{phase}}
Date: {{date}}
Purpose: {{purpose}}
"""
import pytest
from sqlmodel import Session, create_engine
from {{module_path}} import {{ModelName}}
from tests.factories import make_{{model_name}}

@pytest.fixture
def {{model_name}}_data():
    """Fixture providing test data for {{model_name}}"""
    return {
        # Add test data fields here
        # "name": "Test {{ModelName}}",
        # "description": "Test description",
    }

@pytest.fixture
def {{model_name}}_instance({{model_name}}_data):
    """Fixture providing {{model_name}} instance"""
    return {{ModelName}}(**{{model_name}}_data)

class Test{{ModelName}}:
    """Test cases for {{ModelName}} model"""
    
    def test_{{model_name}}_creation(self, {{model_name}}_instance):
        """Test {{model_name}} creation"""
        assert {{model_name}}_instance is not None
        # Add specific assertions here
        # assert {{model_name}}_instance.name == "Test {{ModelName}}"
    
    def test_{{model_name}}_to_dict(self, {{model_name}}_instance):
        """Test {{model_name}} to_dict method"""
        data = {{model_name}}_instance.to_dict()
        assert isinstance(data, dict)
        assert "id" in data
        # Add specific field assertions here
    
    def test_{{model_name}}_from_dict(self, {{model_name}}_data):
        """Test {{model_name}} from_dict method"""
        instance = {{ModelName}}.from_dict({{model_name}}_data)
        assert isinstance(instance, {{ModelName}})
        # Add specific field assertions here
    
    def test_{{model_name}}_repr(self, {{model_name}}_instance):
        """Test {{model_name}} string representation"""
        repr_str = repr({{model_name}}_instance)
        assert "{{ModelName}}" in repr_str
        assert "id=" in repr_str
    
    # Add more test methods as needed
    # def test_{{model_name}}_validation(self):
    #     """Test {{model_name}} field validation"""
    #     pass
    
    # def test_{{model_name}}_relationships(self):
    #     """Test {{model_name}} relationships"""
    #     pass

